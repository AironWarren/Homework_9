#include "figure.h"

namespace mathOperations
{
	int per(figure::Figure&);
	int sq(figure::Figure&);
	double rasst(point::Point, point::Point);
}

namespace outsideWithOperations
{
	void read(figure::Figure&);
	void show(int);
	void show(double);
	double comparisson(figure::Figure, figure::Figure);
}